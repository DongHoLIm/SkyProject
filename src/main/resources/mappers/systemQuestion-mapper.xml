<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SystemQuestion">
	<resultMap type="com.kh.finalProject.board.model.vo.SystemQuestion" id="systemQuestionResultSet">
		<id property="questionNo" column="QUESTION_NO"/>
		<result property="questionType" column="QUESTION_TYPE"/>
		<result property="questionTitle" column="QUESTION_TITLE"/>
		<result property="questionContent" column="QUESTION_CONTENT"/>
		<result property="questionWriter" column="QUESTION_WRITER"/>
		<result property="questionDate" column="QUESTION_DATE"/>
		<result property="questionStatus" column="QUESTION_STATUS"/>
		<result property="questionCount" column="QUESTION_COUNT"/>
		<result property="memberId" column="MEMBER_ID"/>
		<result property="answerNo" column="ANSWER_NO"/>
		<result property="answerTitle" column="ANSWER_TITLE"/>
		<result property="answerContent" column="ANSWER_CONTENT"/>
		<result property="answerWriteDept" column="ANSWER_WRITEDEPT"/>
		<result property="answerWriter" column="ANSWER_WRITER"/>
		<result property="answerDate" column="ANSWER_DATE"/>
		<result property="answerStatus" column="ANSWER_STATUS"/>
		<result property="answerCount" column="ANSWER_COUNT"/>
		<result property="answerGroup" column="ANSWER_GROUP"/>
		<result property="answerStep" column="ANSWER_STEP"/>
		<result property="answerIndent" column="ANSWER_INDENT"/>
	</resultMap>
	
	<!-- 게시글 수 조회  -->
	<select id="systemQuestionListCount" resultType="_int">
		SELECT COUNT(*)
		FROM SYSTEMQUESTION Q
		JOIN SYSTEMQUESTION_ANSWER A ON(Q.QUESTION_NO = A.QUESTION_NO)
		WHERE Q.QUESTION_STATUS = 'Y' AND Q.MEMBER_ID = #{memberId}
	</select>
	
	<!-- 페이징 처리 된 목록 조회 -->
	<select id="systemQuestionList" resultMap="systemQuestionResultSet">
		SELECT *
		FROM SYSTEMQUESTION Q
		JOIN SYSTEMQUESTION_ANSWER A ON(Q.QUESTION_NO = A.QUESTION_NO)
		WHERE Q.QUESTION_STATUS = 'Y' AND Q.MEMBER_ID = #{memberId}
		ORDER BY Q.QUESTION_NO ASC, A.ANSWER_GROUP DESC
	</select>	
	
	<select id="systemQuestionSearchListCount" resultType="_int">
		SELECT COUNT(*)
		FROM SYSTEMQUESTION
		WHERE QUESTION_STATUS = 'Y' AND MEMBER_ID = #{memberId}
		<choose>
			<when test="account != null">
				AND QUESTION_TYPE = '계정 관련'
			</when>
			<when test="proof != null">
				AND QUESTION_TYPE = '제증명 관련'
			</when>
			<otherwise>
				AND QUESTION_TYPE = '학사 관련'
			</otherwise>
		</choose>		
	</select>
	
	<select id="systemQuestionSearchList" resultMap="systemQuestionResultSet">
		SELECT *
		FROM SYSTEMQUESTION
		WHERE QUESTION_STATUS = 'Y' AND MEMBER_ID = #{memberId}
		<choose>
			<when test="account != null">
				AND QUESTION_TYPE = '계정 관련'
			</when>
			<when test="proof != null">
				AND QUESTION_TYPE = '제증명 관련'
			</when>
			<otherwise>
				AND QUESTION_TYPE = '학사 관련'
			</otherwise>
		</choose>	
		ORDER BY QUESTION_NO DESC
	</select>
	
</mapper>